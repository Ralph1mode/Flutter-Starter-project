import "package:flutter/material.dart"; 
 
class usecaseController extends StatelessWidget { 
  @override 
  Widget build(BuildContext context) { 
    return Scaffold( 
      appBar: AppBar( 
        title: Text('usecase'), 
      ), 
      body: Center( 
        child: Text('usecase Controller'), 
      ), 
    ); 
  } 
} 
